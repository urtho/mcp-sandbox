[project]
name = "mcp-sandbox"
version = "0.1.0"
description = "Safe sandboxes for llms"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "docker>=7.1.0",
    "fastapi>=0.115.12",
    "fastmcp>=2.2.0",
    "pydantic>=2.11.3",
    "tomli>=2.2.1",
    "uvicorn>=0.34.0",
]

[project.scripts]
mcp-sandbox = "main:main"

[dependency-groups]
dev = [
    "ruff>=0.11.6",
]
